/** Free users can play this many puzzles per track (by sort_order). */
export const ASCENSION_FREE_PUZZLES_PER_TRACK = 3;

/** Premium members can access at least this many puzzles per track. */
export const ASCENSION_PREMIUM_PUZZLES_PER_TRACK = 20;

/** Minimum level slots shown in admin per track. */
export const ASCENSION_ADMIN_MIN_LEVELS = ASCENSION_PREMIUM_PUZZLES_PER_TRACK;
